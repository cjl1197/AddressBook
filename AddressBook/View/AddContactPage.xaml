<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:fontAwesome="clr-namespace:AddressBook.Helpers"
             x:Class="AddressBook.View.AddContactPage"
             xmlns:model="clr-namespace:AddressBook.Models"
             xmlns:viewmodel="clr-namespace:AddressBook.ViewModels"
             x:DataType="viewmodel:PeopleVM"
             Title="AddContactPage">
    <Grid
        Grid.ColumnDefinitions="*, *"
        Grid.RowDefinitions="*, Auto">

        <VerticalStackLayout
            Grid.Row="0"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            Padding="0,100,0,0">
        <Border
            Grid.Row="0"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            StrokeThickness="2"
            StrokeShape="Rectangle 40"
            Margin="45,5"
            HeightRequest="50"
            HorizontalOptions="FillAndExpand">
            
            <HorizontalStackLayout>
                <Image
                    HorizontalOptions="Start"
                    Margin="20, 0">
                    <Image.Source>
                        <FontImageSource 
                            Glyph="{x:Static fontAwesome:FontAwesomeIcons.User}"
                            Color="{AppThemeBinding Light={StaticResource PrimaryDark}, Dark={StaticResource Primary}}"
                            FontFamily="FAS"
                            Size="24"/>
                    </Image.Source>
                </Image>
                <Entry 
                    Grid.Row="0"
                    Grid.ColumnSpan="2"
                    Placeholder="Name"
                    Text="{Binding FullName, Mode=TwoWay}"
                    ClearButtonVisibility="WhileEditing"
                    BackgroundColor="blue"
                    VerticalOptions="Center" 
                    HorizontalOptions="Center" />
            </HorizontalStackLayout>
        </Border>
            
        <Border
            Grid.Row="1"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            StrokeThickness="2"
            StrokeShape="Rectangle 40"
            Margin="45,5"
            HeightRequest="50"
            HorizontalOptions="FillAndExpand">
            
            <HorizontalStackLayout>
                <Image
                    HorizontalOptions="Start"
                    Margin="20, 0">
                    <Image.Source>
                        <FontImageSource 
                            Glyph="{x:Static fontAwesome:FontAwesomeIcons.Phone}"
                            Color="{AppThemeBinding Light={StaticResource PrimaryDark}, Dark={StaticResource Primary}}"
                            FontFamily="FAS"
                            Size="24"/>
                    </Image.Source>
                </Image>
                <Entry 
                    Grid.Row="1"
                    Grid.Column="0"
                    Grid.ColumnSpan="2"
                    Placeholder="Phone Number"
                    ClearButtonVisibility="WhileEditing"
                    Keyboard="Telephone"
                    VerticalOptions="Center" 
                    HorizontalOptions="Center" />
            </HorizontalStackLayout>
        </Border>
        <Border
            Grid.Row="2"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            StrokeThickness="2"
            StrokeShape="Rectangle 40"
            Margin="45,5"
            HeightRequest="50"
            HorizontalOptions="FillAndExpand">
            
            <HorizontalStackLayout>
                <Image
                    HorizontalOptions="Start"
                    Margin="20, 0">
                    <Image.Source>
                        <FontImageSource 
                            Glyph="{x:Static fontAwesome:FontAwesomeIcons.Envelope}"
                            Color="{AppThemeBinding Light={StaticResource PrimaryDark}, Dark={StaticResource Primary}}"
                            FontFamily="FAS"
                            Size="24"/>
                    </Image.Source>
                </Image>
                <Entry 
                    Grid.Row="1"
                    Grid.Column="0"
                    Grid.ColumnSpan="2"
                    Placeholder="Email"
                    Keyboard="Email"
                    ClearButtonVisibility="WhileEditing"
                    VerticalOptions="Center" 
                    HorizontalOptions="Center" />
            </HorizontalStackLayout>
        </Border>
        <Border
            Grid.Row="3"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            StrokeThickness="2"
            StrokeShape="Rectangle 40"
            Margin="45,5"
            HeightRequest="50"
            HorizontalOptions="FillAndExpand">
            
            <HorizontalStackLayout>
                <Image
                    HorizontalOptions="Start"
                    Margin="20, 0">
                    <Image.Source>
                        <FontImageSource 
                            Glyph="{x:Static fontAwesome:FontAwesomeIcons.LocationDot}"
                            Color="{AppThemeBinding Light={StaticResource PrimaryDark}, Dark={StaticResource Primary}}"
                            FontFamily="FAS"
                            Size="24"/>
                    </Image.Source>
                </Image>
                <Entry 
                    Grid.Row="1"
                    Grid.Column="0"
                    Grid.ColumnSpan="2"
                    Placeholder="Address"
                    ClearButtonVisibility="WhileEditing"
                    VerticalOptions="Center" 
                    HorizontalOptions="Center" />
            </HorizontalStackLayout>
        </Border>
        </VerticalStackLayout>        
        <Button 
            Grid.Row="1"
            Grid.Column="0"
            x:Name="AddBtn"
            IsEnabled="{Binding IsNotBusy}"
            Text="Add"
            Command="{Binding AddContactCommand}"
            Margin="5"/>
        <Button 
            Grid.Row="1"
            Grid.Column="1"
            x:Name="CancelBtn"
            Text="Cancel"
            Command="{Binding PopModalCommand}"
            Margin="5"/>
    </Grid>
</ContentPage>
